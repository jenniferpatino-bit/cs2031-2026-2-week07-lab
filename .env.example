DB_HOST=localhost
DB_PORT=5432
DB_NAME=week07-lab
DB_USER=postgres
DB_PASSWORD=postgres

# Clave HS256 (minimo 32 bytes). Genera una propia con: openssl rand -hex 32
JWT_SECRET=cambia-esta-clave-por-una-de-al-menos-32-bytes-0000
JWT_EXPIRATION_ACCESS=3600000

# Carpeta donde se generan los emails de confirmacion (flight_booking_email_<id>.txt)
BOOKING_EMAIL_DIR=emails
